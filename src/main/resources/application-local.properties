spring.application.name=gridee-parking
spring.application.version=0.0.1-SNAPSHOT

# ==================== Environment-Specific Configuration ====================
# Database Configuration (Local MongoDB)
spring.data.mongodb.uri=mongodb://localhost:27017/parkingdb

# Basic Auth (for local development only)
spring.security.user.name=rajeev
spring.security.user.password=parking

# SSL Configuration (Local)
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=Trishu@123
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=springboot

# ==================== Payment Gateway (Local/Test) ====================
razorpay.key=rzp_test_RS3SKqbTQFAamy
razorpay.secret=AzppbgQ92XdFdX1ADFg4C2hC

# ==================== OAuth2 Credentials (Local) ====================
spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET

spring.security.oauth2.client.registration.apple.client-id=YOUR_APPLE_CLIENT_ID
spring.security.oauth2.client.registration.apple.client-secret=YOUR_APPLE_CLIENT_SECRET

# ==================== Tracing Configuration (Local) ====================
# OPTION 1: Enable both Zipkin and OTLP (if running locally)
management.zipkin.tracing.enabled=false
management.zipkin.tracing.endpoint=http://localhost:9411/api/v2/spans

management.otlp.tracing.enabled=false
management.otlp.tracing.endpoint=http://localhost:4318/v1/traces

# ==================== Environment Tags (Local) ====================
management.metrics.tags.environment=local
management.metrics.tags.region=us-east-1
app.environment=local
app.region=us-east-1
app.cluster=local-dev
app.instance-id=${random.uuid}
